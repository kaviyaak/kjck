#include<stdio.h>
#include<conio.h>
void main()
{
intn,i,count=0;
int x,y;
printf("enter the no");
scanf("%d",&n);
printf("enter the interval");
scanf(%d%d",&x,&y);
if(i>=x&&i<=y)
{
for(i=2;i<n;i++)
{
ifelse((n%i)==0)
{
count++;
}}
if(count==0)
{
printf("prime");
}
else
{
pintf("not a prime")
}
}
